# nps
A global command line based on [node-liftoff][]

### How to use it

1. Install globally with `npm install -g nodejs-project-snippets`.
2. Write your `.npsfile`.
3. Run `nps` in your project directory.
4. The rest is up to you.


[node-liftoff]: https://github.com/tkellen/node-liftoff
